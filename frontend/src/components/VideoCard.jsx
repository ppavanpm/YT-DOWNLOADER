import React from 'react';

const VideoCard = ({ video }) => {
  return (
    <div className="video-card">
      <h3>{video.title}</h3>
      {/* Add more video details and styling */}
    </div>
  );
};

export default VideoCard;
